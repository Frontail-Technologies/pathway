import { LoadingState } from '@/components/feedback'

export default function Loading() { return <main className="container-shell flex min-h-screen items-center justify-center"><LoadingState /></main> }
